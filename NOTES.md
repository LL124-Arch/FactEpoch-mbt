# 开发手记：先把两个时间说清楚

9 月 5 日开仓时，我把检索、摘要、遗忘、日志、CLI 和抽取边界都列进了第一版。它们各自都合理，但放在初赛仓库里反而让主问题不明显：这个项目真正想验证的是双时态事实能不能在 MoonBit 里保持简单。

9 月 7 日做完一轮可运行检查后，我把实现收回到三个文件。旧代码没有被改写历史，仍能从提交记录找到；当前分支只留下模型、写入和查询。这个删除很多代码的提交不是“完成版”，而是一次范围判断。

现在最满意的是晚到修正测试。它只用五个事件，就能证明 `valid_at=160` 时，`known_at=140` 和 `known_at=200` 应得到不同答案。这个例子比继续增加命令数量更能说明项目价值。

还有几处没有磨平：

- `Episode` 里叫 `text`，上游习惯叫 `content`；暂时保留，因为示例读起来更自然。
- `Fact::new` 的参数仍偏长，特别是可选 `valid_to` 后面的来源 Episode，后续可能改成 builder。
- 查询路径测试最完整；撤回只覆盖实现和编译，还缺独立的边界用例。
- 当前线性扫描适合原型，没有做 10,000 事件基准，性能数字不能先写在 README 里。

下一步是否做日志 codec，要看评审更关心可嵌入 API 还是可演示的文件工具。两者不会同时开工。
